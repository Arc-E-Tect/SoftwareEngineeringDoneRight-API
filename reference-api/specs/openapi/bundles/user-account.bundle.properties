# The version of the user-account bundle: its OpenAPI document and, where it has one,
# its AsyncAPI document. Semantic: major for a breaking change, minor for an
# additive one, patch for anything else. Bump it in the same change as the
# fragments it describes.
version=1.0.0
